# 宝塔面板 → 网站 → 设置 → 伪静态 → 粘贴以下内容
# 然后保存并重载 Nginx

location / {
    try_files $uri $uri/ /index.php?$query_string;
}

# 若上面无效，可改用这一版：
# location / {
#     try_files $uri $uri/ /index.php$is_args$args;
# }
